{
  "name": "terrasense-api",
  "version": "1.0.0",
  "description": "TerraSense Node.js API — receives ESP32 vibration data and writes to Firestore",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.3",
    "firebase-admin": "^12.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  }
}
